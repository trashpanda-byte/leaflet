# This file documents variable names only. Never add real values.
# Add entries only when the imported application actually requires them.

# Public browser-safe variables may use the framework's public prefix.
# Private keys, service-role keys, database passwords, and AI provider keys
# must remain server-side and must never use a public prefix.
