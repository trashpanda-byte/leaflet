# TASK-0001 independent review — 2026-09-20

## 2026-09-22 development-readiness assessment

**Mobile foundation works; TASK-0002 is not yet ready to start.** Chris now confirms that his iPhone 15 opens Leaflet and displays the development-foundation shell, including `Expo development client ready to configure`. That is expected placeholder copy in `App.tsx`. This is user-observed physical-device evidence, not a screenshot or direct device inspection by Codex. Exact iOS version has been requested and remains missing from the final handoff. Codex also observed Metro finish an iOS bundle (707 modules, 15755 ms); its `/status` endpoint reports `packager-status:running`.

Readiness checks rerun by Codex on 2026-09-22: Node 22.13.0/npm 10.9.2, typecheck PASS, Expo Doctor 21/21 PASS, repository validation PASS, clean/synchronized starting branch at `8095cc8`, and [CI run 35645902284](https://github.com/trashpanda-byte/leaflet/actions/runs/35645902284) PASS. `npm audit --json` still exits 1 with 10 moderate, 0 high, and 0 critical findings; no forced downgrade was made. The supplied device identifier is not in tracked files, and only `.env.example` is tracked among environment files.

Claude's authentication was checked and a real read-only Claude assessment ran in the existing implementation session. Its useful readiness findings were the missing device-test metadata, a stale CI reference (corrected), and local database prerequisites. Role boundaries remain as defined in CLAUDE.md/AGENTS.md: Claude implements, Codex independently reviews; user-authorized setup/remediation is allowed. Do not mistake commit-author identity alone for evidence that either agent ran. The session output and role-specific evidence are the basis for attribution.

Local database prerequisite gap: no Docker/Podman executable was found on PATH, no standard Docker Desktop installation or matching installed-app entry was found, and `wsl --status` explicitly reports WSL is not installed. The repository has no Supabase CLI dependency yet. [Supabase's local-development guide](https://supabase.com/docs/guides/local-development/cli/getting-started) requires a Docker-compatible container runtime; a normal Windows setup uses [Docker Desktop with WSL 2](https://docs.docker.com/desktop/setup/install/windows-install/). Machine setup may require administrator access and a reboot. No OS installation or Supabase initialization was performed as part of this verification.

Before TASK-0002: finish the exact iOS-version record, complete TASK-0001 review/merge, and prepare the container runtime. Pin the Supabase CLI and verify database reset/RLS tests as part of TASK-0002. No Seed implementation starts before TASK-0001 is merged. Concurrent agent work should use separate checkouts and exact-commit handoffs; never overlap ownership of the same files. Sequential work in this checkout is sufficient. This dated assessment supersedes older statements below that the physical launch is wholly unconfirmed.

## 2026-09-21 signing follow-up

Codex reviewed Claude's commit `1a02870e3254cf696f42ea4075d3666417f55dec`: the sole application configuration change records `ios.infoPlist.ITSAppUsesNonExemptEncryption=false`, generated by EAS for the current data-free shell. No custom encryption code or dependency exists in the scaffold; reassess the declaration if that changes. Typecheck, Doctor (21/21), and repository validation passed in Claude's actual output; Codex's diff/whitespace review passed. [GitHub CI run 35645426451](https://github.com/trashpanda-byte/leaflet/actions/runs/35645426451) passed for this commit. No new blocking code finding.

Apple activation is no longer the blocker: Codex's EAS invocation successfully authenticated, registered the app identifier, created managed signing credentials and ad hoc provisioning, and included the user-supplied registered iPhone. Internal development build [1797c499-f96b-4c1f-a808-b08c3af48d1e](https://expo.dev/accounts/trashpandadev/projects/leaflet/builds/1797c499-f96b-4c1f-a808-b08c3af48d1e) was submitted. Uploaded source was `6f5478f` plus the exact app configuration change subsequently committed in `1a02870`; application source/configuration now matches the uploaded source. No credential material or device identifier is committed.

Codex confirmed build status `FINISHED` at `2026-09-21T19:33:01.369Z` using `npx eas-cli@latest build:view 1797c499-f96b-4c1f-a808-b08c3af48d1e --json`. EAS returned an IPA artifact; build logs show successful completion hooks and artifact upload. This is a real signed native build, not merely JS export. Metro is listening on port 8090; `/status` reports `packager-status:running`. Installation and physical-device rendering remain unverified. The original review below is dated evidence, superseded by this follow-up where Apple/signing/build status differs. TASK-0001 remains incomplete and TASK-0002 remains untouched.

## Result and scope

**PASS for the non-device foundation; INCOMPLETE REVIEW for TASK-0001 overall.** No BLOCKING or HIGH code defect found. Required signed iPhone build/install/launch evidence is missing, so PR #3 must remain draft and unmerged. TASK-0002 implementation remains prohibited until TASK-0001 is verified and merged.

Reviewer: Codex. Implementer: authenticated local Claude Code (session `83259913-f518-4886-9d27-499bd6905412`), which actually ran checks, edited setup/configuration documentation, and produced commits `5a5b72b` and `55eedaf`. Codex located/cloned the repository, coordinated the work, performed the user-authorized EAS linking, reviewed the implementation, and corrected evidence in this documentation-only follow-up. This is not a claim of an additional automated Codex CLI run.

Reviewed target: `55eedaf982d797b7a7254e9e2e47c490914ac83c` on `feat/application-foundation`; base `main` at `726373931b72259ffb708d879e6aee9964c60b57`. Inspected both the full PR diff and the follow-up from `aeab1c8`, including app entry/shell, package manifest/lockfile, TypeScript/app/EAS configuration, CI, validation script, instructions, domain boundaries, and active/queued tasks.

## Findings and remaining gate

- **Required evidence gap:** no signed physical-iPhone development build, installation, or Metro-to-device launch has run. Apple enrollment is user-reported pending, not independently queried. Once active, register the iPhone, create/install the development build, and record device model/iOS version, EAS build ID, fresh-clone commands, and observed Leaflet shell. A JS export or Metro ready message does not satisfy this gate.
- **Known dependency limitation:** `npm audit --json` reports 10 moderate, 0 high, and 0 critical findings. All trace to GHSA-w5hq-g745-h8pq in `uuid@7.0.3`, through `xcode@3.0.1` and Expo build/config tooling. `xcode/lib/pbxProject.js:90` calls `uuid.v4()` without a buffer; the advisory concerns v3/v5/v6 with a supplied buffer. No affected call path was found in this scaffold. This scoped assessment does not fix the dependency or make the audit clean. The offered forced Expo 46 downgrade was not applied. Owner: the TASK-0001 maintainer; revisit when compatible upstream tooling changes, and reassess before adding new callers.
- Handoff inaccuracies (tracked-file count, fresh-clone provenance, attribution, and final check evidence) were corrected in the review follow-up. No product-code remediation was needed.

## Reproducible verification

Environment: Windows, Node `v22.13.0`, npm `10.9.2`. Codex prepended `C:\Users\tolle\.fnm\node-versions\v22.13.0\installation` to the process PATH; no machine-wide runtime change was made.

| Command / inspection | Result and attribution |
|---|---|
| `git clone --branch feat/application-foundation https://github.com/trashpanda-byte/leaflet.git leaflet` | Codex created a fresh clone at `aeab1c8`; old unrelated local clone was left unchanged. |
| `npm install` | Claude: 474 packages installed. Cosmetic optional-package metadata churn discarded. Lockfile was already committed in `aeab1c8`. |
| `npm ci` | Claude: 474 packages installed, no lockfile drift; command output independently inspected by Codex. |
| `npm run typecheck` | Claude and Codex: exit 0, no TypeScript errors. Claude reran after final app configuration. |
| `npm run doctor` | Claude and Codex: 21/21 passed. Claude reran after final app configuration. |
| `bash -n scripts/validate-repository.sh` | Claude and Codex: exit 0. Codex used Git for Windows Bash. |
| `bash scripts/validate-repository.sh` | Both: exit 0, repository contract valid. Rerun after final app configuration and review documentation. |
| `npm ls --depth=0` | Claude: expected seven direct dependencies, exit 0; output inspected by Codex. |
| `npm audit --json`, `npm explain uuid` | Codex independently confirmed the limitation above; audit exits nonzero. |
| `npx expo start --dev-client --offline --port 8090` | Claude: `Waiting on http://localhost:8090`, then stopped its server. Default 8081 was occupied; no existing server was terminated. |
| `npx expo export --platform ios --output-dir ../work/codex-ios-export` | Codex reran against final `55eedaf` configuration: exit 0, 582 modules, 1.4 MB Hermes bundle. This is not a native build. |
| `npx eas-cli@latest whoami` | Codex: authenticated as `trashpandadev`. |
| `npx eas-cli@latest init --account trashpandadev --non-interactive --no-icon` | Codex, after Chris selected that account: created and linked `@trashpandadev/leaflet`, ID `0dd168ed-e23c-475f-830d-32190618a508`. |
| `npx eas-cli@latest project:info` | Codex: confirmed project/account/ID. |
| `npx eas-cli@latest config --platform ios --profile development --non-interactive` | Codex reran against final `55eedaf`: exit 0; `com.trashpandadev.leaflet`, internal distribution, development client, remote credentials configuration. No credentials or build were created. |
| `git diff --check origin/main...HEAD` | Codex: exit 0. |

Remote CI baseline: [run 35533800411](https://github.com/trashpanda-byte/leaflet/actions/runs/35533800411) at `aeab1c8` passed all configured steps, independently inspected through GitHub job results. Follow-up CI evidence is recorded below and on [PR #3](https://github.com/trashpanda-byte/leaflet/pull/3); every subsequently pushed commit must receive its own passing run.

Follow-up: [run 35535508313](https://github.com/trashpanda-byte/leaflet/actions/runs/35535508313) at `55eedaf982d797b7a7254e9e2e47c490914ac83c` completed successfully. Codex verified the run and job steps through GitHub. This covers the final EAS/bundle configuration and Claude's setup documentation. The documentation-only review commit will also be pushed and its CI checked before the user handoff; the exact final run is linked on PR #3 to avoid a self-referential evidence commit.

### Secret scan scope

Codex inspected 50 tracked files at `aeab1c8`: `.env.example` contains comments only; no private environment, generated dependency/native, or signing-key filenames were tracked. The repository validator's credential-pattern scan passed. An additional `git rev-list --all` scan covered 67 commits using quiet `git grep -I -q -E` checks for Anthropic/OpenAI/Supabase/GitHub token formats, AWS access-key IDs, and private-key headers; zero matching commits. Neither scan printed potential secret values. These are scoped pattern/filename checks, not proof that every possible secret format is detectable.

Repeat the history check in PowerShell (exit 0 means a match, 1 means none; any other code is a scan failure):

```powershell
$pattern = '(sk-ant-[A-Za-z0-9_-]{16,}|sk-proj-[A-Za-z0-9_-]{16,}|sb_secret_[A-Za-z0-9_-]{16,}|gh[pousr]_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{40,}|AKIA[A-Z0-9]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----)'
foreach ($sha in (git rev-list --all)) {
  git grep -I -q -E $pattern $sha -- . ':!scripts/validate-repository.sh' ':!docs/ai/reviews/TASK-0001.md'
  if ($LASTEXITCODE -eq 0) { throw "Potential credential in commit $sha; inspect privately" }
  if ($LASTEXITCODE -ne 1) { throw "Secret scan failed" }
}
```

## Acceptance and maintainability

Stack/runtime/layout, committed lockfile, minimal scope, typecheck/Doctor, scaffold CI, safe `.env.example`, setup docs, and focused cleanup have evidence. Fresh-clone installation is verified. All combined device criteria remain unchecked.

No unnecessary dependency, dead product subsystem, speculative abstraction, oversized responsibility, or domain/UI coupling was found. The existing tiny palette is appropriately local. No larger refactor is required before the next layer once device verification and merge complete. The shell uses the system font fallback; production typography/icon work is outside this scaffold.

No Supabase/auth/schema, Seed logic, model SDK, embedding, prompt, or generative runtime was added. Database/RLS/cross-user tests and AI necessity checks are N/A for the current data-free shell; they remain required for the queued domain work. No application user data or raw private content is logged. The EAS project ID and bundle identifier are public configuration, not secrets.

Lint, formatting, unit/integration tests, and platform-native build commands are not configured or not applicable to this tiny scaffold on Windows; no passing results are claimed for them. UI layout, VoiceOver, large text, actual native startup, and device interaction remain untested until the iPhone is available. The static shell has no network-driven loading/error state or animation to test. No physical-device result, Apple activation, production change, or TASK-0002 implementation is claimed.
